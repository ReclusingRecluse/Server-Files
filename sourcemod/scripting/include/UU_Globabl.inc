//UU Global Stuffs

new String:Attrib[32];

public Action:Timer_UpgradCalc(Handle:Timer, any:iEnt)
{
	if (IsValidEdict(iEnt))
	{
		{
			if (StrContains(Attrib, "", true) != -1)
		}
	}
	for (new client = 0; client < MaxClients; client++)
	{
		if (iEnt == client && IsValidClient(client))
		{
		}
	}
}
			

			